# tuples like as same as list but its immuatable, we cant re assign value to them

tup = (1, 2, 3, 4, 5, 5, 5, 5, 5, 5)
print(tup)
print(type(tup))

# Methods in tuples

# 01 Index method
tup = (1, 2, 3, 4, 5, 5, 5, 5, 5, 5)
print(tup.index(3)) # its method show us to when first time in the tup and which one index eliment come


# 02 Index method
tup = (1, 2, 3, 4, 5, 5, 5, 5, 5, 5)
print(tup.count(5)) # count the value of element